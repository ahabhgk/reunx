(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports, require('react')) :
  typeof define === 'function' && define.amd ? define(['exports', 'react'], factory) :
  (global = global || self, factory(global.reunx = {}, global.react));
}(this, (function (exports, react) { 'use strict';

  var createX = function createX(useHook) {
    for (var _len = arguments.length, initialArgs = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      initialArgs[_key - 1] = arguments[_key];
    }

    var Context = react.createContext(null);

    var Provider = function Provider(_ref) {
      var children = _ref.children;
      var value = useHook.apply(void 0, initialArgs);
      return react.createElement(Context.Provider, {
        value: value
      }, children);
    };

    return {
      Provider: Provider,
      Context: Context
    };
  };

  var useX = function useX(x) {
    return react.useContext(x.Context);
  };

  var combineX = function combineX() {
    for (var _len2 = arguments.length, xs = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      xs[_key2] = arguments[_key2];
    }

    return xs.reduce(function (Acc, Cur) {
      return function (_ref2) {
        var children = _ref2.children;
        return react.createElement(Acc.Provider, null, react.createElement(Cur.Provider, null, children));
      };
    });
  };

  exports.combineX = combineX;
  exports.createX = createX;
  exports.useX = useX;

  Object.defineProperty(exports, '__esModule', { value: true });

})));
